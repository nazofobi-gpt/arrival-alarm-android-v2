{
  "name": "arrival-alarm-connector-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "start": "node src/http-server.js",
    "test": "node --test",
    "preflight:production": "node scripts/production-preflight.js",
    "check:worker": "wrangler deploy --dry-run --config wrangler.ci.jsonc",
    "check:d1": "wrangler d1 migrations apply arrival-alarm-ci --local --config wrangler.ci.jsonc"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.1",
    "zod": "4.6.5",
    "jose": "6.2.12"
  },
  "devDependencies": {
    "wrangler": "4.141.0"
  }
}
